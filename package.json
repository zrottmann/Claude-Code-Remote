{
  "name": "claude-code-remote-appwrite",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'Using pre-built static files'",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "appwrite": "^18.2.0"
  }
}
